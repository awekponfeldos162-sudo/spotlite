name: spotlite
description: Spotlite - La preuve par l'action
publish_to: 'none'

environment:
  sdk: '^3.10.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  flutter_riverpod: ^2.5.1
  go_router: ^14.0.0
  flutter_dotenv: ^5.1.0
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1
  timeago: ^3.6.1
  uuid: ^4.4.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env